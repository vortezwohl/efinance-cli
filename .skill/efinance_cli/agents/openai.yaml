interface:
  display_name: "efinance CLI"
  short_description: "解释并调用 efinance-cli，默认推荐 JSON 输出与 full 指标级别"
  default_prompt: "使用 $efinance-cli 解释或规划一个 efinance-cli 命令，默认优先建议 --format json 和 --indicator-level full，并说明真实输出结构、参数和排查方式。"

policy:
  allow_implicit_invocation: true
